import React from 'react';
import PropTypes from 'prop-types';
import {StyledForm} from './Form.style';

const Form = (props) => {
    return (
        <StyledForm {...props}>
            
        </StyledForm>
    );
};

Form.propTypes = {
    
};

export default Form;